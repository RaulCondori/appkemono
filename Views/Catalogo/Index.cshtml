@model IEnumerable<appcomics.Models.Producto>
@{
<<<<<<< HEAD
    ViewData["Title"] = "Catalogo";  
}

<h1 align="center">@ViewData["Title"]</h1>
<br><br>
  <div class="container" align="center">
=======
    ViewData["Title"] = "NUESTRO CATALOGO";  
}

<h1><center>@ViewData["Title"]</center></h1>
<br>
  <div class="container" style="text-align:center; width:70% height:20%" >
>>>>>>> 9d384e8b6d7e71600f5a5a9320713cde9c70395f
    <h1>@ViewData["Message"]</h1>
    <div class="row">
      <div class="col">
        <div id="products" class="mb-3">
          <div class="row">
            @foreach (var item in Model) {
            <div class="col-sm-3">
              <div class="card mb-3">
                <div class="card-body">
                  <div class="card-img-actions">
                    <img class="img-responsive" width="150" height="150"
                       src="/images/@item.ImageName" alt="@item.ImageName"  />
                  </div>
                </div>
                <div class="card-body">
                  <h5 class="card-title">@Html.DisplayFor(modelItem => item.Name)</h5>
                  <p class="card-text">S/. @Html.DisplayFor(modelItem => item.Precio)</p>
                  <a class="btn btn-primary" asp-action="Add" asp-route-id="@item.Id">Add</a>
                  <a class="btn btn-info" asp-action="Details" asp-route-id="@item.Id">View</a>
                </div>
              </div>
            </div>
            }
         </div>
        </div>
      </div>
    </div> 
  </div>